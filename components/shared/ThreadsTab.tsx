import React from 'react'

type ThreadsTabProps = {
  currentUserId: string;
  accountId: string;
  accountType: string;
};

const ThreadsTab = ({
  currentUserId,
  accountId,
  accountType,
}: ThreadsTabProps) => {

    // fecth profile Threads
  return (
    <div>ThreadsTab</div>
  )
};

export default ThreadsTab